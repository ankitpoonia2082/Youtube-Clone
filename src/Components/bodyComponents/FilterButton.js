// Filter Button...

const FilterButton = ({ name }) => {

    return (
        <button
            className="bg-gray-100 rounded-xl mx-3 px-3 py-2"
        >{name}</button>
    );
};

export default FilterButton;